machine:
  node:
    version: 6.1.0

general:
  branches:
    ignore:
      - gh-pages
test:  
  override:
    - npm run test-ci
    
deployment:
  staging:
    branch: master
    commands:
      - npm run build
      - ./deploy-ghpages.sh .
