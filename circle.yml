machine:
  node:
    version: 6.1.0

general:
  branches:
    only:
      - master

dependencies:
  pre:
    - npm install -g webpack

test:
  override:
    - exit 0

deployment:
  staging:
    branch: master
    commands:
      - npm run build
      - ./deploy-ghpages.sh .
