machine:
  node:
    version: 6.1.0

general:
  branches:
    only:
      - master

test:
  override:
    - exit 0

deployment:
  staging:
    branch: master
    commands:
      - npm run build
      - ./deploy-ghpages.sh .
